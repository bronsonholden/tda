FILE(
	GLOB
	TDA_HEADER_FILES
	${TDA_INCLUDE_DIR}/*.h
	*.h
	)

FILE(
	GLOB
	TDA_SOURCE_FILES
	*.c
	)

ADD_EXECUTABLE(
	TDA
	${TDA_HEADER_FILES}
	${TDA_SOURCE_FILES}
	)

TARGET_LINK_LIBRARIES(
	TDA
	TDA_fs
	TDA_hid
	TDA_math
	TDA_mm
	)
